{
  "name": "@feedbackagent/db",
  "version": "0.0.1",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "dev": "tsc --watch",
    "generate": "prisma generate",
    "db:push": "DATABASE_URL=\"file:./prisma/dev.db\" prisma db push",
    "migrate": "DATABASE_URL=\"file:./prisma/dev.db\" prisma migrate dev",
    "studio": "DATABASE_URL=\"file:./prisma/dev.db\" prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.13.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "prisma": "^5.13.0",
    "typescript": "^5.4.0"
  }
}
